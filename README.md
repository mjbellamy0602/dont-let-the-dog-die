# Don't Let the Dog Die

Static promotional website for *Don't Let the Dog Die*, a debut novel by MJ Bellamy.

## Local preview

Open `index.html` in a browser, or serve the directory with any static HTTP server.

## Deployment

The GitHub Actions workflow in `.github/workflows/deploy-pages.yml` deploys the site to GitHub Pages whenever `main` is updated.
